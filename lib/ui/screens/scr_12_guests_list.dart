// SCR-12 Guests List — phase 03 placeholder.
import 'package:flutter/material.dart';
import 'package:kasaran/ui/screens/placeholder_screen.dart';

class Scr12GuestsList extends StatelessWidget {
  const Scr12GuestsList({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderScreen(
        screenId: 'SCR-12',
        screenName: 'Guests List',
      );
}
