// SCR-07 Ledger List — phase 03 placeholder.
import 'package:flutter/material.dart';
import 'package:kasaran/ui/screens/placeholder_screen.dart';

class Scr07LedgerList extends StatelessWidget {
  const Scr07LedgerList({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderScreen(
        screenId: 'SCR-07',
        screenName: 'Ledger List',
      );
}
