// SCR-18 Plan Settings — phase 03 placeholder.
import 'package:flutter/material.dart';
import 'package:kasaran/ui/screens/placeholder_screen.dart';

class Scr18PlanSettings extends StatelessWidget {
  const Scr18PlanSettings({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderScreen(
        screenId: 'SCR-18',
        screenName: 'Plan Settings',
      );
}
