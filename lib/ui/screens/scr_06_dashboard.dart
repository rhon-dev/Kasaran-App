// SCR-06 Dashboard — phase 03 placeholder.
// Phase 15 implements the full bento layout.
// The AI insights placeholder tile (ux-spec §4.5) is NOT created in phase 03.
import 'package:flutter/material.dart';
import 'package:kasaran/ui/screens/placeholder_screen.dart';

class Scr06Dashboard extends StatelessWidget {
  const Scr06Dashboard({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderScreen(
        screenId: 'SCR-06',
        screenName: 'Dashboard',
      );
}
