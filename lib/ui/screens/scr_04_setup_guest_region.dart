// SCR-04 Setup: Guest Cap & Region — phase 03 placeholder.
import 'package:flutter/material.dart';
import 'package:kasaran/ui/screens/placeholder_screen.dart';

class Scr04SetupGuestRegion extends StatelessWidget {
  const Scr04SetupGuestRegion({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderScreen(
        screenId: 'SCR-04',
        screenName: 'Setup: Guest Cap & Region',
      );
}
