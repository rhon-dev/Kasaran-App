// SCR-10 Allocations & Overrides — phase 03 placeholder.
import 'package:flutter/material.dart';
import 'package:kasaran/ui/screens/placeholder_screen.dart';

class Scr10Allocations extends StatelessWidget {
  const Scr10Allocations({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderScreen(
        screenId: 'SCR-10',
        screenName: 'Allocations & Overrides',
      );
}
