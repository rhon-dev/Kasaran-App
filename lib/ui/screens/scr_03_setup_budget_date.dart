// SCR-03 Setup: Budget & Date — phase 03 placeholder.
// Phase 11 implements the real onboarding form.
import 'package:flutter/material.dart';
import 'package:kasaran/ui/screens/placeholder_screen.dart';

class Scr03SetupBudgetDate extends StatelessWidget {
  const Scr03SetupBudgetDate({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderScreen(
        screenId: 'SCR-03',
        screenName: 'Setup: Budget & Date',
      );
}
