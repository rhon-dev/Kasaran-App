// SCR-01 Sign In / Sign Up — phase 03 placeholder.
// Phase 06 replaces this with the real Supabase Auth implementation.
import 'package:flutter/material.dart';
import 'package:kasaran/ui/screens/placeholder_screen.dart';

class Scr01SignIn extends StatelessWidget {
  const Scr01SignIn({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderScreen(
        screenId: 'SCR-01',
        screenName: 'Sign In / Sign Up',
      );
}
